# ZukoLabs VTO — Python Dependencies

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Configuration
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Supabase
supabase>=2.9.0

# Groq LLM
groq==0.11.0

# Replicate (Try-On Engine)
replicate>=1.0.0

# HTTP Client
httpx==0.27.2

# Image Processing
Pillow==10.4.0
numpy>=1.26.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# Utilities
python-dateutil==2.9.0

# NOTE: mediapipe is optional — install separately if needed for
# face/hand landmark detection (AR overlays for jewelry/eyewear).
# On Windows, mediapipe has heavy dependencies (jax, jaxlib).
# pip install mediapipe
